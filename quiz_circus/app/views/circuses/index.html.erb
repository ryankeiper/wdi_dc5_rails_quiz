<h1>Circuses!</h1>
<% @circuses.each do |circus| %>
<p><%= link_to circus.name, circus_path(circus) %></p>
<div><%= image_tag circus.image_url %></div>
<% end %>

<p><%= link_to "Add a Circus", new_circus_path %></p>